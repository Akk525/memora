{
  "name": "@memora/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "description": "Shared types, canonicalization, and crypto for Memora (internal monorepo only; memora-core is self-contained for npm publish)",
  "license": "MIT"
}